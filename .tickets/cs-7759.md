---
id: cs-7759
status: closed
deps: [cs-wyaa, cs-k2pa]
links: []
created: 2026-02-20T05:34:14Z
type: task
priority: 2
assignee: Michael Barrett
parent: cs-kmv6
---
# Add check_stop max_minutes test coverage

check_stop in session.py has 4 stop conditions: _shutdown_requested, max_turns, max_minutes, auto_complete. The max_minutes path (session.py lines 391-393) has ZERO test coverage.

FIX: Add tests to the appropriate test file (after cs-wyaa splits test_cli.py, TestCheckStop should live in test_session.py):

  def test_max_minutes_exceeded(self):
      config = SessionConfig(session_id='t', topic='t', max_turns=20, max_minutes=1)
      # start_time 2 minutes ago exceeds 1-minute limit
      result = check_stop(config, turn=1, start_time=time.time() - 120)
      assert result == 'max_minutes'

  def test_max_minutes_not_exceeded(self):
      config = SessionConfig(session_id='t', topic='t', max_turns=20, max_minutes=60)
      result = check_stop(config, turn=1, start_time=time.time())
      assert result is None

Check the actual check_stop signature before writing tests — the above is illustrative.

Files touched: test_session.py (after split from cs-wyaa)

